@using Portfolio.Shared.Model

@if (Items == null)
{
    <p>読み込み中...</p>
}
else
{
    @foreach (var work in Items)
    {
        <WorkHistory Item="work" />
    }
}

@code {
    [Parameter]
    public List<WorkHistoryItem>? Items { get; set; }
}